import React from "react";
import {Redirect} from "react-router-dom";
import "./stylesProfileAuth.scss";

const ProfileContainer = () => <Redirect to="/my-web-site/login"/>

export default ProfileContainer;
